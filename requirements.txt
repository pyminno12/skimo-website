streamlit>=1.37
pandas
plotly

# ==========================================
# 영구 DB(Postgres) 연결에 필요한 패키지
# secrets.toml에 [connections.skimo_db]를 설정해 사용하는 경우 필수입니다.
# 설정하지 않으면 로컬 JSON 파일로 자동 대체되지만,
# Streamlit Cloud 등 배포 환경에서는 회원 정보가 주기적으로 초기화됩니다.
# ==========================================
sqlalchemy>=2.0
psycopg2-binary

# ==========================================
# (선택) 이메일/SMS 본인 인증(OTP) 기능에 사용
# ==========================================
# twilio  # SMS 발송을 사용하려면 주석 해제 후 설치
